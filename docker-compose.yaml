services:
  ba-assemble-nemplads-to-sql:
    container_name: ba-assemble-nemplads-to-sql
    # image: 'ghcr.io/hjk-automatisering/ba-assemble-nemplads-to-sql:main' # Uncomment on portainer
    build: . # Remove on portainer
    command: sleep infinity
    restart: always
    volumes:
      - filer-assemble-nemplads:/shr/AssembleNemplads
    env_file:
      - .env # stack.env on portainer
    environment:
      - TZ=Europe/Copenhagen
    labels:
      ofelia.enabled: "true"
      ofelia.job-exec.ba-assemble-nemplads-to-sql.schedule: "${CRON_SCHEDULE}"
      ofelia.job-exec.ba-assemble-nemplads-to-sql.container: "ba-assemble-nemplads-to-sql"
      ofelia.job-exec.ba-assemble-nemplads-to-sql.command: "python main.py"

  ofelia:
    image: mcuadros/ofelia:latest
    container_name: ofelia
    restart: always
    command: daemon --docker
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      - ba-assemble-nemplads-to-sql

volumes:
  filer-assemble-nemplads:
    external: true